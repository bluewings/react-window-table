---
name: use cases
---

import { useState } from 'react';
import { Props } from 'docz';
import { WindowTable } from '../src/core';
import { columns, rows } from './resources/data/fake-users.js'
import { Styled } from './resources';

# Use Cases

## contentWidth < sum(fixedWidth)

### shrink

<WindowTable
        height={300}
        columns={columns}
        rows={rows}
        rowCount={10}
        rowHeight={46}
        columnCount={10}
        columnWidth={150}
        fixedTopCount={1}
        fixedLeftCount={2}
        fixedRightCount={1}
        fixedBottomCount={0}
        overscanCount={2}
        guideline={true}
        renderHeader={(column) => {
          return  column
        }}
        theme={(classNames) => {
          return {
            [classNames.CELL]: {
              display: 'flex',
              fontSize: 12.5,
              padding: 8,
              borderRight: '1px solid #dadcde',
              alignItems: 'center'
            },
            [classNames.ROW_EVEN]: {
              background: '#f5f6f7'
            },
            [classNames.IS_SCROLLING]: {
              [classNames.CELL1]: {
                background: 'lightyellow'
              },
            },
            [classNames.SECTION_TOP]: {
              [classNames.CELL]: {
                background: '#f5f6f7',
                borderBottom: '1px solid #d3d3d3',
                fontWeight: 700
                
              },
            },
            
          }
        }}
        events={{
          click: {
            '.icon, span': (event, ui) => {
              console.log('>>>> wow i got a click')
              console.log('> event', event)
              console.log('> ui', ui)
              alert(ui.data.firstName);
            }
          },
          mouseover: {
            '.cell': (event, ui) => {
              window._cnt1 = (window._cnt1 || 0);
              console.log('hover', window._cnt1++)
            }
          },
          mouseout: {
            '.cell': (event, ui) => {
              window._cnt2 = (window._cnt2 || 0);
              console.log('out', window._cnt2++)
            }
          },
        }}
      />


## theme

## events

## isScrolling

## hover style

## column text-align / ellipsis / className