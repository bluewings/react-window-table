---
name: Examples
---

import { useState } from 'react';
import { Props } from 'docz';
import { Styled } from './resources';
import { WindowTableCore } from '../src/core';

# Use Cases

## scrollWidth < clientWidth

### shrink

<Styled>
<WindowTableCore
  height={300}
  rowCount={100}
  rowHeight={30}
  columnCount={5}
  columnWidth={100}
  fixedTopCount={3}
  fixedLeftCount={1}
  fixedRightCount={1}
  fixedBottomCount={3}
  overscanCount={2}
  fillerColumn='shrink'
>
{({ columnIndex, rowIndex, className, style }) => (
  <div className={className} style={style}>
    {rowIndex} , {columnIndex}
  </div>
)}
</WindowTableCore>
</Styled>

### dummy 열 추가

<Styled>
<WindowTableCore
  height={300}
  rowCount={100}
  rowHeight={30}
  columnCount={5}
  columnWidth={100}
  fixedTopCount={2}
  fixedLeftCount={1}
  fixedRightCount={1}
  fixedBottomCount={4}
  overscanCount={2}
  fillerColumn={true}
>
{({ columnIndex, rowIndex, className, style }) => (
  <div className={className} style={style}>
    {rowIndex} , {columnIndex}
  </div>
)}
</WindowTableCore>
</Styled>

### 마지막 열의 너비를 늘임

<!-- ### 마지막 열의 너비를 늘임 -->
<Styled>
<WindowTableCore
  height={300}
  rowCount={100}
  rowHeight={30}
  columnCount={5}
  columnWidth={100}
  fixedTopCount={2}
  fixedLeftCount={1}
  fixedRightCount={1}
  fixedBottomCount={4}
  overscanCount={2}
  fillerColumn='stretch'
>
{({ columnIndex, rowIndex, className, style }) => (
  <div className={className} style={style}>
    {rowIndex} , {columnIndex}
  </div>
)}
</WindowTableCore>
</Styled>

### 비워둠 none

<!-- ### 마지막 열의 너비를 늘임 -->
<Styled>
<WindowTableCore
  height={300}
  rowCount={100}
  rowHeight={30}
  columnCount={5}
  columnWidth={100}
  fixedTopCount={2}
  fixedLeftCount={1}
  fixedRightCount={1}
  fixedBottomCount={4}
  overscanCount={2}
  fillerColumn='none'
>
{({ columnIndex, rowIndex, className, style }) => (
  <div className={className} style={style}>
    {rowIndex} , {columnIndex}
  </div>
)}
</WindowTableCore>
</Styled>

## scrollHeight < clientHeight

<!--WindowTableCore
  height={200}
  rowCount={5}
  rowHeight={30}
  columnCount={100}
  columnWidth={100}
  fixedTopCount={1}
  fixedLeftCount={1}
  fixedRightCount={1}
  fixedBottomCount={1}
  overscanCount={0}
>
{({ columnIndex, rowIndex, style }) => (
  <div className="cell" style={style}>
    {rowIndex} , {columnIndex}
  </div>
)}
</WindowTableCore-->

## Basic usage

<!--WindowTableCore
  height={300}
  rowCount={5}
  rowHeight={30}
  columnCount={5}
  columnWidth={100}
  fixedTopCount={1}
  fixedLeftCount={1}
  fixedRightCount={1}
  fixedBottomCount={1}
  overscanCount={0}
>
{({ columnIndex, rowIndex, style }) => (
  <div className="cell" style={style}>
    {rowIndex} , {columnIndex}
  </div>
)}
</WindowTableCore-->

