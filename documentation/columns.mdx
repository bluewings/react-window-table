---
name: Columns
route: /columns
---

import { useState } from 'react';
import { Props } from 'docz';
import { Playground } from './resources';
import WindowTable from '../src/core/components/WindowTable/WindowTable';
// import { columns, rows } from './resources/data/fake-users.js'
import mona from './resources/assets/mona.jpg';
import useImage from './resources/hooks/useImage';

import { columns, rows } from './resources/data/pokedex.js';

# Introduction


## Basic usage

<Playground>
{() => {
  const columns = ['name', 'height', 'weight', 'type'];
  const rows = [
    ['Bulbasaur', 0.71, 6.9, 'Grass, Poison'], 
    ['Ivysaur', 0.99, 13, 'Grass, Poison'], 
    ['Venusaur', 2.01, 100, 'Grass, Poison'], 
    ['Charmander', 0.61, 8.5, 'Fire'], 
    ['Charmeleon', 1.09, 19, 'Fire'], 
    ['Charizard', 1.7, 90.5, 'Fire, Flying'], 
    ['Squirtle', 0.51, 9, 'Water'], 
    ['Wartortle', 0.99, 22.5, 'Water'], 
    ['Blastoise', 1.6, 85.5, 'Water'], 
    ['Caterpie', 0.3, 2.9, 'Bug'],
  ];
  return <WindowTable columns={columns} rows={rows} />;
}}
</Playground>


## Properties

<Props of={WindowTable} />

<!-- [Currently, the Props component is not working.](https://github.com/pedronauck/docz/issues/717) -->
